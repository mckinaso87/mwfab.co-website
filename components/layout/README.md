# layout

Shell components: Header, Footer, and any wrapper that appears on every page.

## Components

- **Header** — Logo, nav links, Request a Bid CTA, mobile menu.
- **Footer** — Service area, key links.

## Conventions

- Use semantic `<header>`, `<nav>`, `<footer>`.
- Logo and nav should work on dark gradient backgrounds.
