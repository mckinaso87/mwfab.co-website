# Open Graph default image

`default.png` is a temporary 1200×630 placeholder generated from the logo.

TODO: Replace `default.png` with final branded OG artwork (1200×630, readable at small preview sizes).
